# Sample Program: Number Guessing Game
import random

number = random.randint(1, 10)  # computer picks a number
guess = int(input("Guess a number between 1 and 10: "))

if guess == number:
    print("🎉 Correct! You guessed the number.")
else:
    print("❌ Wrong! The number was", number)
